import org.jerkar.tool.JkBuild;

// Just here to generate an iml file for this projet so it appears in hierachical view in IntelliJ
// But to build the whole project you still have to run 'master' build located in master sub directory.
class ParentBuild extends JkBuild {
}
